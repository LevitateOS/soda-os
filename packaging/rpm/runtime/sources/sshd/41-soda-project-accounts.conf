# Soda people authenticate as themselves. The forced gateway validates the
# optional project selector against their root-created personal workspace.
AcceptEnv SODA_PROJECT
Match Group soda-people
    AuthorizedKeysFile /etc/soda/authorized_keys/%u
    ForceCommand /usr/libexec/soda/soda-ssh
Match all
