# Project members authenticate as the shared project Unix account, but
# only root may select the forced soda-ssh command attached to their key.
Match User soda-p-*
    AuthorizedKeysFile /etc/soda/authorized_keys/%u
Match all
