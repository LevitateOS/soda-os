table inet soda_ingress {
    chain input {
        type filter hook input priority -10; policy accept;

        iifname { "lo", "tailscale0" } tcp dport { 22, 9090, 30000 } accept
        tcp dport { 22, 9090, 30000 } reject with tcp reset
    }
}
