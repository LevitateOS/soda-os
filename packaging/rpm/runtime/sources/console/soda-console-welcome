#!/bin/sh
set -eu

printf '\nWelcome to Soda OS.\n'

soda-tailnet

address=$(ip -o -4 addr show scope global 2>/dev/null | awk 'NR == 1 { sub(/\/.*/, "", $4); print $4 }')
if [ -n "$address" ]; then
	printf '\nLocal console address:\n  https://%s:9090\n' "$address"
fi
